# -*- coding: utf-8 -*-
"""云迹·数据源适配器包。

每个适配器 = 一个数据源，职责单一：**源格式 → 契约B比赛记录**（字段名见
docs/data-contracts.md 契约B，值为字符串，日期规范化为 YYYY-MM-DD，其余保持原文）。

接口约定：每个适配器模块必须实现 `fetch() -> list[dict]`（每 dict 一行比赛记录，
键为契约B字段名）。新增/更换数据源 = 在本包新增一个模块实现 fetch()，其余零改动。
"""
